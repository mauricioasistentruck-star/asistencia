{
  "name": "asistentruck-app",
  "version": "1.0.0",
  "description": "ASISTENTRUCK - Inversiones BOTAM SpA",
  "main": "server/server.js",
  "scripts": {
    "build": "npm install && cd client && npm install && npm run build && cd ../server && npm install",
    "start": "node server/server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "socket.io": "^4.8.3",
    "sqlite3": "^5.1.7",
    "xlsx": "^0.18.5"
  }
}